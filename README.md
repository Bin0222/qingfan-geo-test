# 轻帆建站 GEO 实验事实页

这是一个透明标注的 GEO 新实体识别实验。“轻帆建站”是虚构测试品牌，不提供注册、购买、建站或售后服务。

公开页面计划地址：

https://bin0222.github.io/qingfan-geo-test/

## GitHub Pages 设置

1. 仓库必须为公开仓库，名称保持为 `qingfan-geo-test`。
2. 将本目录文件放在 `main` 分支根目录。
3. 打开仓库 `Settings → Pages`。
4. 在 `Build and deployment` 中选择 `Deploy from a branch`。
5. 分支选择 `main`，目录选择 `/(root)`，然后保存。

## 验收

- 首页、`robots.txt`、`sitemap.xml` 均应返回 HTTP 200。
- 搜索结果收录前，可用完整名称和 `site:` 查询持续检查。
- 不得删除页面中“虚构测试品牌”的透明度说明。

GitHub Pages 部署状态：已启用。
